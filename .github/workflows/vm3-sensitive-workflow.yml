name: VM3 sensitive workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inert:
    runs-on: ubuntu-latest
    steps:
      - run: echo VM3_SENSITIVE_WORKFLOW_BASE

# VM3 midflight permission TOCTOU inert marker 2026-09-17
